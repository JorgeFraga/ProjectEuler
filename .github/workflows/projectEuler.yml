name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: setup
      run: dir C:\"Program Files (x86)\Microsoft Visual Studio"\2019\Enterprise\VC\Tools\MSVC\
    - name: make
      run: CL Problem1.cpp
    
    
